{
  "name": "supercut",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main/index.js",
  "scripts": {
    "dev": "vite build --config vite.renderer.config.ts && electron .",
    "dev:renderer": "vite --config vite.renderer.config.ts",
    "build:main": "tsc -p tsconfig.main.json",
    "build": "npm run build:main && vite build --config vite.renderer.config.ts",
    "start": "electron ."
  },
  "dependencies": {
    "electron-updater": "^6.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "electron": "^43.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
